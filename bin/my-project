#!/usr/bin/env ruby

# This is the file where you would initiate your cli application if that is what you were building
# Something similar to the example below.
# To execute this file whilst developing use bundle exec.
# If its installed as a gem call it directly like an executable.
require 'my_project/cli'

MyProject::CLI.run
